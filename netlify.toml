[build]
  command = "npm run build"
  publish = ".next"
  ignore = "git diff HEAD^ HEAD --name-only | grep -qEv '(\\.md$|LICENSE$|\\.env\\.example$|^\\.github/|^\\.vscode/)' && exit 1 || exit 0"

[build.environment]
  NEXT_PUBLIC_API_BASE_URL = "https://myadmin.techparkit.info"
